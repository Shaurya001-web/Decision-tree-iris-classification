# Decision-tree-iris-classification
This project implements a Decision Tree Classifier using the Iris dataset.  It includes data preprocessing, model training with max_depth=2, prediction,  accuracy evaluation, and visualization of the decision tree.   The model achieves 98% accuracy .
